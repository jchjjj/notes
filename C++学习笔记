C++学习
成员函数后的const表示，当本class的const instance调用该方法的重载形式时，会优先调用const版本；
同时，这个函数不会改变此类内部的成员变量或数据；

##新的C++ 工具##
Clang：新的C族语言编译工具链，以编译速度和友好的编译提示信息出名。
biicode：跨平台的C++库依赖管理工具；
UndoDB：可以让程序“倒退”的调试器；
Clion：JetBrain出的C/C++ IDE；
OSv：C++写的操作系统，为云平台而优化，
